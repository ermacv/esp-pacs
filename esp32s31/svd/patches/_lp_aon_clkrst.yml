_modify:
  RTC_SAR2_PWDET_CCT:
    description: >-
      SOURCE[S31_ESP_PACS_BASE_SVD,ROM_REV0_PHY_POWER_DETECTOR];
      CONFIDENCE[exact-register-and-instruction-use]. LP always-on SAR2 power
      detector circuit control. Complete ESP32-S31 rev0 ROM
      phy_pwdet_reg_init and phy_pwdet_sar2_init select encoding 4; complete
      phy_txcal_debuge_mode_ selects encoding 2.

RTC_SAR2_PWDET_CCT:
  _modify:
    RTC_SAR2_PWDET_CCT:
      description: >-
        SOURCE[S31_ESP_PACS_BASE_SVD,ROM_REV0_PHY_POWER_DETECTOR];
        CONFIDENCE[exact-field-and-instruction-use]. Three-bit SAR2 power
        detector circuit encoding. Complete ESP32-S31 rev0 ROM uses values 4
        for initialization/enabling and 2 for TX-calibration debug mode.
